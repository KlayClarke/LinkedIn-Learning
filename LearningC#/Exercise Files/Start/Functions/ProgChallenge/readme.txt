Use this folder for the Programming Challenge.

Create a new .NET console app by running the command:

dotnet new console

In your terminal in this folder.

RULES:
- Program reads input and determines if palindrome
- Must ignore case and punctuation
- Function must be named IsPalindrome and must return two values: Bool and integer
- Program runs until "exit" is typed